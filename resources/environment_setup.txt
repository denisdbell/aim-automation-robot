*** Settings ***
Documentation     A resource file with reusable keywords and variables.
...               This file contains variables and keywords which are used to setup
...               and configure tests
Library           Selenium2Library
Library           Collections
Library           OperatingSystem
Resource          aim_common.robot

*** Variables ***
#Environment settings
${ENVIRONMENT}    dev    #Dev = dev    | Test = test    | Stage = stage
${TEAM}           NOT_AIM
#Browser
${BROWSER}        firefox    #Supported: chrome | firefox | ie | iphone
#Sleep settings
${NAVIGATION}     6
${EVENT}          2
${DEVICE_LAUNCH}    6
#Routes
${PUBLIC_SITE}    https://${ENVIRONMENT}.fepblue.org
${SERVER}         https://${ENVIRONMENT}.fepblue.org:7443/pilot
${LOGIN_URL}      ${SERVER}/login
${MYBLUE}         https://${ENVIRONMENT}.fepblue.org:8443/
${MYBLUE_LOGIN_URL}    ${MYBLUE}web/guest/myblue
${SERVER_NAVBAR}    ${MYBLUE}html/js/navlib/v1
${CSR}            ${SERVER_NAVBAR}/ESsample.html
#Classic Routes
${REGISTRATION_URL}    ${SERVER}/login
#Setup iPhone
${IPHONE_DESIRED_CAPABILITIES}    device:iPhone Simulator,platformName:iOS,deviceName:iPhone,safari:true,browserName:Safari
${REMOTE_URL}     http://127.0.0.1:4723/wd/hub
${ALIAS}          ip71
${TEST_NAME}
${TEST TAGS}
${SELENIUM_DEVICE}

*** Keywords ***
Environment setup
    #________________ Setup global variables _________________#
    Run Keyword If    '${ENVIRONMENT}'=='dev'    develop
    Run Keyword If    '${ENVIRONMENT}'=='test'    test
    Run Keyword If    '${ENVIRONMENT}'=='stage'    stage
    Run Keyword If    '${ENVIRONMENT}'=='local'    local
    Run Keyword If    '${ENVIRONMENT}'=='prod'    prod
    Run Keyword If    '${TEAM}'=='AIM'    Initialize Google Bearer Token

develop
    #SSO Configuration
    Set global variable    ${SSO}    ssodev2
    Set global variable    ${ESERVICE_SSO}    http://custservv2
    Set global variable    ${ESERVICE_PID}    http://custservv2.fepblue.org/fedlogin
    Set global variable    ${CHIPREWARDS_SSO}    fepdemo
    Set global variable    ${CHIPREWARDS_TARGET}    https://${CHIPREWARDS_SSO}.mychiprewards.com
    Set global variable    ${CHIPREWARDS_PID}    https://fepssouat.mychiprewards.com
    Set global variable    ${HEALTH_CLUB_TARGET}    https://fitnessyourway-fep-dev-saml.healthways.com/sp/ACS.saml2
    Set global variable    ${CAREMARK_TARGET}    https://sit1www.caremark.com
    Set global variable    ${CAREMARK_PID}    SP_CVS_FEP_Dev
    Set global variable    ${SSO_PORT}    :9032
    #Routes
    Set global variable    ${SERVER}    https://${ENVIRONMENT}.fepblue.org:8443/pilot
    Set global variable    ${LOGIN_URL}    ${SERVER}/login
    Set global variable    ${MYBLUE}    https://${ENVIRONMENT}.fepblue.org:8443/
    Set global variable    ${MYBLUE_LOGIN_URL}    ${MYBLUE}web/guest/myblue
    Set global variable    ${SERVER_NAVBAR}    ${MYBLUE}html/js/navlib/v1
    Set global variable    ${CSR}    ${SERVER_NAVBAR}/ESsample.html

test
    #SSO Configuration
    Set global variable    ${SSO}    ssotest    #Dev = ssodev2    | Test = ssotest    | Stage = ssodev
    Set global variable    ${ESERVICE_SSO}    https://custservpt    #Dev = http://custservv2    | Test = https://custservpt    | Stage = http://custservv2
    Set global variable    ${ESERVICE_PID}    SBP-ESERVICE-PT    #Test = SBP-ESERVICE-PT    | Dev & Stage = http://custservv2.fepblue.org/fedlogin
    Set global variable    ${CHIPREWARDS_SSO}    feptest    #Dev = fepdemo    | Test = feptest    | Stage = fepuat
    Set global variable    ${CHIPREWARDS_TARGET}    https://${CHIPREWARDS_SSO}.mychiprewards.com
    Set global variable    ${CHIPREWARDS_PID}    https://feptest.mychiprewards.com    #Dev = fepssouat    | Test = feptest    | Stage = fepuat
    Set global variable    ${HEALTH_CLUB_TARGET}    https://fitnessyourway-fep-dev-saml.healthways.com/sp/ACS.saml2
    Set global variable    ${CAREMARK_TARGET}    https://sit1www.caremark.com
    Set global variable    ${CAREMARK_PID}    SP_CVS_FEP_Dev
    Set global variable    ${SSO_PORT}    :9031    #Dev = 9032    | Test = 9031    | Stage = 9031

stage
    #SSO Configuration
    Set global variable    ${SSO}    ssodev    #Dev = ssodev2    | Test = ssotest    | Stage = ssodev
    Set global variable    ${ESERVICE_SSO}    http://custservv2    #Dev = http://custservv2    | Test = https://custservpt    | Stage = http://custservv2
    Set global variable    ${ESERVICE_PID}    http://custservv2.fepblue.org/fedlogin    #Test = SBP-ESERVICE-PT    | Dev & Stage = http://custservv2.fepblue.org/fedlogin
    Set global variable    ${CHIPREWARDS_SSO}    fepuat    #Dev = fepdemo    | Test = feptest    | Stage = fepuat
    Set global variable    ${CHIPREWARDS_TARGET}    https://${CHIPREWARDS_SSO}.mychiprewards.com
    Set global variable    ${CHIPREWARDS_PID}    https://fepuat.mychiprewards.com    #Dev = fepssouat    | Test = feptest    | Stage = fepuat
    Set global variable    ${HEALTH_CLUB_TARGET}    https://fitnessyourway-fep-dev-saml.healthways.com/sp/ACS.saml2
    Set global variable    ${CAREMARK_TARGET}    https://sit1www.caremark.com
    Set global variable    ${CAREMARK_PID}    SP_CVS_FEP_Dev
    Set global variable    ${SSO_PORT}    :9031    #Dev = 9032    | Test = 9031    | Stage = 9031

local
    #SSO Configuration
    Set global variable    ${SSO}    ssodev2
    Set global variable    ${ESERVICE_SSO}    http://custservv2
    Set global variable    ${ESERVICE_PID}    http://custservv2.fepblue.org/fedlogin
    Set global variable    ${CHIPREWARDS_SSO}    fepdemo
    Set global variable    ${CHIPREWARDS_TARGET}    https://${CHIPREWARDS_SSO}.mychiprewards.com
    Set global variable    ${CHIPREWARDS_PID}    https://fepssouat.mychiprewards.com
    Set global variable    ${HEALTH_CLUB_TARGET}    https://fitnessyourway-fep-dev-saml.healthways.com/sp/ACS.saml2
    Set global variable    ${CAREMARK_TARGET}    https://sit1www.caremark.com
    Set global variable    ${CAREMARK_PID}    SP_CVS_FEP_Prod
    Set global variable    ${SSO_PORT}    :9032
    #Routes
    Set global variable    ${SERVER}    https://192.168.200.113:7443
    Set global variable    ${LOGIN_URL}    ${SERVER}/login
    Set global variable    ${MYBLUE}    https://dev.fepblue.org:8443/
    Set global variable    ${MYBLUE_LOGIN_URL}    ${MYBLUE}web/guest
    Set global variable    ${SERVER_NAVBAR}    ${MYBLUE}html/js/navlib/v1

prod
    #SSO Configuration
    Set global variable    ${SSO}    sso
    Set global variable    ${ESERVICE_SSO}    https://custserv
    Set global variable    ${ESERVICE_PID}    https://custserv.fepblue.org/fedlogin
    Set global variable    ${CHIPREWARDS_SSO}    fepdemo
    Set global variable    ${CHIPREWARDS_TARGET}    https://www.mybluewellness.fepblue.org
    Set global variable    ${CHIPREWARDS_PID}    https://www.mybluewellness.fepblue.org
    Set global variable    ${HEALTH_CLUB_TARGET}    https://fitnessyourway-fep-saml.healthways.com/sp/ACS.saml2
    Set global variable    ${CAREMARK_TARGET}    https://www.caremark.com
    Set global variable    ${CAREMARK_PID}    SP_CVS_FEP_Prod
    Set global variable    ${SSO_PORT}    ${EMPTY}
    #Routes
    Set global variable    ${SERVER}    https://www.fepblue.org/pilot
    Set global variable    ${LOGIN_URL}    ${SERVER}/login
    Set global variable    ${MYBLUE}    https://www.fepblue.org/
    Set global variable    ${MYBLUE_LOGIN_URL}    ${MYBLUE}web/guest/myblue
    Set global variable    ${SERVER_NAVBAR}    ${MYBLUE}html/js/navlib/v1